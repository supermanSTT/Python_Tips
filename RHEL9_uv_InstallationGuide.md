# uv 설치 및 사용 가이드

`uv`는 Astral에서 개발한 **초고속 Python 패키지 및 가상환경 관리
도구**입니다. `pip`, `pip-tools`, `virtualenv`를 대체할 수 있으며, Rust
기반으로 매우 빠릅니다.

------------------------------------------------------------------------

## 1. uv 설치

### macOS / Linux

``` bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

설치 후 터미널을 재시작하거나 아래 명령으로 PATH를 적용합니다.

``` bash
source ~/.cargo/env
```

### Homebrew (macOS)

``` bash
brew install uv
```

### Windows (PowerShell)

``` powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

설치 확인:

``` bash
uv --version
```

------------------------------------------------------------------------

## 2. 프로젝트 초기화

``` bash
uv init my-project
cd my-project
```

생성 파일: - `pyproject.toml` - `.python-version`

------------------------------------------------------------------------

## 3. 가상환경 생성 및 실행

``` bash
uv venv
```

가상환경 활성화:

``` bash
source .venv/bin/activate   # macOS/Linux
.venv\Scripts\activate    # Windows
```

------------------------------------------------------------------------

## 4. 패키지 설치

### 패키지 추가

``` bash
uv add fastapi
```

### 개발용 의존성

``` bash
uv add --dev pytest
```

### requirements.txt 설치

``` bash
uv pip install -r requirements.txt
```

------------------------------------------------------------------------

## 5. 의존성 동기화

``` bash
uv sync
```

-   `pyproject.toml` 기준으로 정확히 재현
-   lock 파일 자동 관리

------------------------------------------------------------------------

## 6. Python 실행

``` bash
uv run python main.py
```

또는

``` bash
uv run pytest
```

------------------------------------------------------------------------

## 7. pip 대체 명령어 비교

  기존 pip        uv
  --------------- ---------------
  pip install     uv add
  pip uninstall   uv remove
  pip freeze      uv pip freeze
  virtualenv      uv venv

------------------------------------------------------------------------

## 8. uv 사용 장점

-   ⚡ 매우 빠른 설치 속도
-   🔒 lock 기반 재현성
-   🧩 pip / venv 완전 호환
-   🛠 하나의 도구로 통합 관리

------------------------------------------------------------------------

## 9. 권장 사용 시나리오

-   FastAPI / Django 백엔드
-   AI / ML 프로젝트
-   CI/CD 환경
-   로컬 LLM 및 에이전트 서버

------------------------------------------------------------------------

## 10. 참고 자료

-   공식 문서: https://docs.astral.sh/uv/
-   GitHub: https://github.com/astral-sh/uv

------------------------------------------------------------------------

Happy coding 🚀
